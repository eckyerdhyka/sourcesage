# Technical Challenge - Python Backend

## Step install
- python3 -m venv venv
- source venv/bin/activate
- pip install -r requirements.txt
if get any error for for psycopg (export path)

- export FLASK_APP=server
- flask run